// import { loader as auditLogLoader } from './auditLogController';
// import * as models from '../models/core';

// describe('auditLogController.loader', () => {
//   it('should return an array of PriceAuditLog objects (empty by default)', async () => {
//     const mockRequest = {} as any;
//     const response = await auditLogLoader({ request: mockRequest });
//     const data = await response.json();
//     expect(Array.isArray(data.auditLogs)).toBe(true);
//     expect(data.auditLogs.length).toBe(0);
//   });
// });
